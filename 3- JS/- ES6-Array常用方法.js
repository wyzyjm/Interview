/**
 数组常用方法:
    
    基础:
        shift()
        unShift()
        pop()
        push()
        sort()
        reverse()   反转
        splice(start,length)
    迭代:
        map((item,index)=>{} )
        forEach((item,index)=>{})
        reduce((total,pre,next)=>{},init)

        find()
        findIndex()
        some()
        every()
        filter()
 */